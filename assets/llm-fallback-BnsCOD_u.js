var e=`장애 원인 판정에 LLM 을 쓰는 제일 쉬운 방법은 로그를 통째로 넣고 "원인이 뭐야" 라고 묻는 것입니다. 데모에서는 그럴듯한데, 두 가지가 안 됩니다.

- 같은 장애에 다른 답이 나올 수 있습니다. 판정이 재현되지 않으면 복구 자동화의 근거로 쓸 수 없습니다
- 왜 그렇게 판단했는지 감사할 수 없습니다

## 순서를 뒤집었다

기본 판정은 규칙 엔진이 합니다. LLM 은 **규칙이 매칭되지 않은 장애에만** 호출됩니다. 아는 문제에 모델 비용과 불확실성을 쓰지 않습니다.

\`\`\`
장애 --> 규칙 엔진 매칭 ──잡힘──> 판정 (LLM 호출 없음)
                    └──미매칭──> LLM 에 후보 요청 --> 같은 근거 평가 --> 통과한 후보만 채택
\`\`\`

## LLM 이 할 수 있는 일을 좁혔다

호출할 때도 자유 서술을 받지 않습니다.

- **제안은 카탈로그에 존재하는 원인 ID 로 제한**합니다. 카탈로그에 없는 원인을 지어내면 그 답은 버려집니다
- 응답 형식은 JSON Schema 로 강제합니다. 후보는 최대 5개, 각 후보는 \`cause_id\` 와 confidence 만 갖습니다
- 프롬프트에는 근거 원문 대신 수집 시점의 요약만 싣습니다. 토큰을 줄이는 동시에, 로그에 섞인 비밀 값이 모델로 나가는 경로를 막습니다

그리고 가장 중요한 것: **LLM 이 낸 후보도 규칙과 똑같은 근거 평가를 통과해야** 결론이 됩니다. 근거 없는 후보는 confidence 가 높아도 차단됩니다. 모델의 확신이 아니라 증거가 판정을 만듭니다.

## 제공자는 설정으로 바꾼다

호출부는 포트 하나만 봅니다. OpenAI · Anthropic · Gemini · OpenAI 호환 서버를 환경 변수로 교체할 수 있고, 에이전트 코드는 바뀌지 않습니다. 재시도 대상 상태 코드와 타임아웃도 게이트웨이가 관리합니다.

## 확인할 수 있는 것

- 후보 생성 파이프라인: \`src/services/ai/agent/pipeline/ai_fallback.py\`
- 응답 스키마와 후보 상한: 같은 파일의 \`FALLBACK_CANDIDATES_SCHEMA\`
- 게이트웨이: \`src/packages/ai/llm.py\`

## 한계

LLM 후보의 실제 채택률과 규칙 대비 품질은 아직 측정하지 못했습니다. 규칙 미매칭 장애 표본이 쌓여야 잴 수 있는 값입니다.`;export{e as default};