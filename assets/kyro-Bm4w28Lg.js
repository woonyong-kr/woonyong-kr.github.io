var e=`**AI 에게 복구를 맡기되, 판단은 규칙 · 경계 · 평가로 묶었습니다.** 판정 시나리오 **116개** 자동 평가 accuracy **94.3% → 100%** · 오탐 **0%** · 권한 경계 계약 테스트 **45종** · 이벤트 왕복 **30배** 실측 — 전부 로컬 Docker 로 재현됩니다.

Kyro 는 Kubernetes 에 익숙하지 않은 사용자가 GitOps 기반으로 장애를 복구할 수 있게 돕는 서비스입니다. 장애가 발생하면 운영 데이터를 모아 원인을 판정하고, 수정안을 Draft PR 로 올린 뒤 실제로 복구됐는지 확인합니다. 5인 팀으로 진행했고, 저는 마이크로서비스 아키텍처와 이벤트 기반 통신, 규칙 기반 원인 분석 엔진, 복구 에이전트의 권한 설계를 맡았습니다.

말보다 실물이 빠릅니다. 크래프톤 정글 공식 채널(Jungle Dev Club)에 올라간 발표 · 시연 영상입니다.

![Kyro — AI Agent 기반 Kubernetes 배포 및 운영 자동화 서비스 (크래프톤 정글 공식 채널)](https://www.youtube.com/watch?v=Ar4rNJZX7lU)

## 풀려던 문제

AI 가 프로덕션을 고치게 해도 되는가. 이 프로젝트의 대답은 "맡기되, 판단의 경계와 검증을 코드로 먼저 만든다"입니다. 아래 모든 설계가 이 한 문장에서 나왔습니다.

## 맡은 일

- 서비스 40여 개를 이벤트로 연결하는 구조 설계 · 구현
- 유실 · 중복 없는 이벤트 전달 구현 (Transactional Outbox · 멱등 · DLQ)
- 장애 원인을 찾는 규칙 엔진 구현과 골든셋 116 시나리오 자동 평가 (accuracy 94.3% → 100%)
- AI 제안은 등록된 원인 87종, 데이터 접근은 읽기 전용으로 제한 (환각 방지 · MCP)
- AI 복구는 사람 승인 후 적용되게 구현 (GitHub Draft PR · 계약 테스트 45종)
- 종료 후 확장: 데이터 정합성 검사(2인 공동) · 구조 · 비용 검증(개인)

## AI 의 권한 — 게이트를 전부 통과해야 Draft PR 하나

**에이전트는 클러스터를 직접 바꿀 수 없습니다.** 복구 경로는 GitHub Draft PR 하나로 단일화했고, 그 앞에 게이트 세 개를 뒀습니다.

![AI 복구의 권한 경계 — 읽기 전용 에이전트, 게이트 3개, Draft PR 강제](images/posts/kyro-trust-boundary.svg)

- **Patch Allowlist** — 바꿀 수 있는 것은 Deployment 의 배포 설정뿐입니다. 허용 action 6종(메모리 · 이미지 롤백 · 태그 수정 · replica · probe · selector)으로 한정하고, 값 범위까지 제한합니다 (replica 1~10, 메모리 상한 4GiB, 롤백은 역패치 완전 일치 강제).
- **Delivery 정책** — 전달 방식이 pull_request 가 아니면 무조건 거부합니다. 코드에 merge API 호출 경로 자체가 없습니다.
- **Base SHA 재검증** — PR 을 만들기 직전 기준 커밋을 다시 확인합니다. 저장소가 그사이 움직였다면 그대로 올리지 않습니다.

경계는 세 층에서 강제됩니다. 에이전트 RBAC 은 get · list · watch 만 부여하고, Helm 렌더 결과에서 금지 리소스와 쓰기 verb 를 정적 게이트로 검사하며, 계약 테스트 45종이 경계를 고정합니다 — 경계가 느슨해지면 빌드가 실패합니다. 자세한 판단 과정은 [복구 권한을 Draft PR 로 좁힌 이유](#/posts/recovery-authority)에 남겼습니다.

## 판정 정확도 94.3% → 100% — 평가가 찾은 결함 5건

**"만들었다"가 아니라 "측정했다"로 끝내고 싶었습니다.** 원인 판정 규칙을 도메인 16종의 YAML 카탈로그(rule 29개 · 원인 후보 87종)로 분리하고, 그 카탈로그를 역산해 골든셋 116 시나리오(장애 87 + 정상 29)를 자동 생성하는 평가 하네스를 만들었습니다.

![평가 루프 — 골든셋 생성, 평가, 결함 수정, 재평가로 accuracy 94.3% 에서 100% 로](images/posts/kyro-eval-loop.svg)

첫 평가는 accuracy 94.3%(82/87)였고, 혼동 5쌍이 나왔습니다. 원인을 추적하니 패턴이 문제였습니다 — 넓은 로그 패턴이 좁은 패턴을 잠식했고, 점수가 동점이면 선언 순서로 승패가 갈렸습니다. 패턴 4곳을 분해 · 축소하고, 판별 신호 그룹을 신설하고, 동점 처리에 "더 구체적인 후보 우선" 규칙을 추가해 100%(87/87) · 오탐 0% 를 만들었습니다. 개선 전후가 커밋 diff 로 남아 있습니다.

규칙이 매칭되지 않은 장애에 한해 LLM 을 보조로 호출합니다. LLM 은 카탈로그에 존재하는 원인 ID 만 제안할 수 있고, 제안된 후보도 규칙과 동일한 근거 평가를 통과해야 채택됩니다. 판정의 기준은 규칙 쪽에 두고, LLM 은 후보를 넓히는 역할만 맡습니다. LLM 호출은 단일 포트로 추상화해 모델 · 지연 · 토큰 · 추정 비용을 호출마다 기록합니다. 규칙을 YAML 로 뺀 이유는 [별도 글](#/posts/rule-catalog)로, 환각을 막는 87종 제한은 [이 글](#/posts/llm-fallback)로 남겼습니다.

## 판정 입력 축소 — 장애에 귀속된 로그만 재집계

**다른 네임스페이스의 노이즈가 진짜 장애를 가리는 문제에서 출발했습니다.** 장애와 무관한 스트림이 판정 입력에 섞이면 원인이 흐려집니다. 장애 네임스페이스 · Pod 에 귀속되는 스트림만 남기고, 라인 수 · 패턴 · 심각도 · trace id 를 선택된 스트림만으로 다시 집계했습니다.

![판정 입력 축소 파이프라인 — 귀속 필터, 재집계, 상한과 바이트 예산](images/posts/kyro-log-scoping.svg)

세 가지 원칙을 지켰습니다. 원본 증거는 그대로 보존하고 판정 번들만 정제합니다. 라벨이 없어 귀속을 판단할 수 없는 스트림은 버리지 않고 보수적으로 유지합니다. 상한(엔트리 8 · 스트림 4 · 텍스트 1,600자)과 바이트 예산을 넘겨 잘라낼 때는 원본 개수를 metadata 로 함께 기록해, 판정이 "얼마나 잘린 데이터인지" 알고 판단하게 합니다.

## 서비스 40여 개를 이벤트로 잇고, 유실 · 중복은 계약으로 막기

**한 서비스가 느려져도 다른 서비스가 함께 멈추지 않아야 합니다.** 도메인별 게이트웨이와 워커 40여 개로 서비스를 나누고, 직접 호출 대신 이벤트로 연결했습니다. 이벤트 이름은 도메인과 행동으로 나누고, 이미 일어난 사실과 처리 요청을 구분했습니다. 전 서비스 이벤트는 단일 NATS JetStream 스트림에 적재하되 보존 기간과 용량 상한을 명시했습니다.

상태는 바뀌었는데 이벤트만 사라지면 뒤따르는 모든 판단이 어긋납니다. [Transactional Outbox](#/posts/transactional-outbox) 로 상태 변경과 같은 트랜잭션에 이벤트를 기록하고, relay 가 읽어 발행합니다. 워커가 가져갈 행에는 임대 시간을 붙여 만료된 것만 회수하고, 메시지 고유 번호로 재발행 중복을 제거하며, 반복 실패한 이벤트는 DLQ 로 모읍니다. 종료 후에는 [AWS 청구서를 분석](#/posts/cost-postmortem)해 전송 계층의 왕복 제거로 30배(2,674 → 82,088 events/s) 빨라짐을 실측했습니다.

Helm 차트로 서비스별 배포를 표준화해 EKS, 일반 Kubernetes 클러스터, 로컬 Docker Compose 세 환경에서 같은 방식으로 기동합니다.

## 복구 확인 — 연속 5분이 유지될 때만 "복구됨"

**PR 이 머지됐다고 복구된 것이 아닙니다.** 배포 후 새 증거를 다시 수집해, 연속 5분의 안정화 창이 유지될 때만 복구로 판정합니다. 수집이 끊기면 연속성을 리셋하고, 복구 전 baseline 이 없으면 fail-closed 로 판정을 보류합니다. 같은 워크로드라도 SLI 시계열의 identity 가 다르면 복구로 인정하지 않습니다. 이 판정 로직은 테스트 27종으로 고정했습니다.

## 운영 데이터 정합성 검사 (종료 후 2인 공동 확장)

프로젝트 종료 후 팀원(이민정)과 둘이서 확장한 계층입니다. 수집이 성공해도 데이터는 어긋납니다. 원천이 필드 타입을 바꾸거나, 같은 날짜가 두 번 적재되거나, 어제부터 아무것도 들어오지 않는 경우입니다. 판정이 쓰는 데이터가 맞는지 매일 확인하는 계층을 만들었습니다.

PostgreSQL 에 자산 · 계약 · 관측 이력 · 계보를 나눠 담고, 유일 제약으로 재실행해도 행이 늘지 않게 고정했습니다. 스키마 변형 · 최신성 · 계보 단절 · 중복 적재를 일일 배치로 검사합니다.

정상 데이터로만 시험하면 무엇이든 통과한다고 답하는 검사도 통과합니다. 원천을 끊고, 필드 타입을 바꾸고, 같은 날짜를 두 번 적재하는 [고장 주입 절차](#/posts/dead-checks)를 함께 만들었고, 이걸로 상시 통과하던 검사 2건을 찾아 고쳤습니다. 검사 질의의 인덱스를 다시 설계해 전체 소요를 424.5ms 에서 150.0ms 로 줄였습니다. MCP 읽기 전용 서버를 붙여 LLM 이 권한 범위 안에서만 카탈로그를 조회하도록 했습니다.

## 더 깊은 기록

가장 어려웠던 세 문제는 각각 글로 남겼습니다.

- [장애 원인 판정을 YAML 로 뺀 이유](#/posts/rule-catalog) — 규칙 엔진과 시나리오 116개 검증
- [LLM 에게 정답 대신 후보를 시키기](#/posts/llm-fallback) — 환각을 막는 87종 제한
- [아무것도 못 잡던 검사 두 개](#/posts/dead-checks) — 고장 주입으로 찾은 결함

## 한계

운영 배포 당시의 트래픽 · 복구 시간 기록은 보존하지 못해 운영 수치로 말하지 않습니다. 전송 계층 벤치마크는 전송만의 비교이며 전체 시스템 처리량이 아닙니다. 지표는 전부 로컬에서 재현 가능한 것만 씁니다.

## 확인하는 방법

백엔드 테스트 203개와 골든셋 평가(accuracy 100% · 오탐 0%)가 로컬 Docker 에서 전부 재현됩니다. AWS 계정도 실제 클러스터도 필요하지 않습니다. 평가는 \`evals/\` 의 골든셋 생성 · 실행 스크립트로, 결과 이력은 \`evals/results.md\` 와 커밋 diff 로 확인할 수 있습니다.`;export{e as default};